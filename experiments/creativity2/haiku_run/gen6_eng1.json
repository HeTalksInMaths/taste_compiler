[
  {
    "name": "formulaic_density_score",
    "causal_node": "cliche_formulaicity",
    "mechanism": "Formulaic Language Index via bigram frequency: ratio of high-frequency bigrams (both component words in top 10% frequency) penalizes clichéd co-occurrences. Score = 1 - (formulaic_bigrams / total_bigrams). Citation: Gablasova et al. 2017 (formulaic sequences); Wray (2002) phraseology.",
    "reasoning": "cliche_formulaicity is explicitly uncovered with no population measurement. Formulaic sequences are by definition high-frequency conventional co-occurrences (causal graph edge: cliche_formulaicity -> novelty, sign=-). Novel creative text should avoid formulaic bigrams. Deterministic calibration: top 10% high-frequency words via wordfreq top_n_list.",
    "code": "import re\nimport numpy as np\nfrom wordfreq import word_frequency, top_n_list\nimport statistics\n\n_W = re.compile(r\"[a-zA-Z']+\")\nSTOP = set((\"a an the and or but if then else of to in on at by for with from as is are was were \"\n    \"be been being it its this that these those i you he she we they me him her us them my your \"\n    \"his their our not no so do does did have has had will would can could should may might must \"\n    \"about into over under out up down off than too very just also\").split())\n\n_HIGH_FREQ_WORDS = set(top_n_list(\"en\", 3000)[:300])\n\ndef _toks(s):\n    return [t for t in _W.findall((s or \"\").lower())]\n\ndef _content(s):\n    return [t for t in _toks(s) if t not in STOP and len(t) > 1]\n\ndef formulaic_density_score(text, anchor):\n    if not text:\n        return 0.5\n    content_toks = _content(text)\n    if len(content_toks) < 2:\n        return 0.5\n    bigrams = [tuple(content_toks[i:i+2]) for i in range(len(content_toks)-1)]\n    if not bigrams:\n        return 0.5\n    formulaic_count = sum(1 for w1, w2 in bigrams if w1 in _HIGH_FREQ_WORDS and w2 in _HIGH_FREQ_WORDS)\n    formulaic_ratio = formulaic_count / len(bigrams)\n    return max(0.0, min(1.0, 1.0 - formulaic_ratio))\n\nMETRICS = {\"formulaic_density_score\": formulaic_density_score}"
  },
  {
    "name": "morphological_concreteness_score",
    "causal_node": "concreteness_imageability",
    "mechanism": "Inverted-U on proportion of abstract-forming morphemes (-ness, -ity, -ment, -tion, -ism, -ship, -age). Score peaks when ~25% of content tokens carry abstract suffixes, rewarding strategic use of abstract terminology within predominantly concrete language. Citation: Brysbaert et al. concreteness norms; morphological complexity in writing quality (Crossley & McNamara).",
    "reasoning": "concreteness_imageability is uncovered with no population scorer. Causal graph shows mixed/low-confidence edge to judged_creativity, suggesting optimal range (not extreme). Creative text balances vivid concrete imagery with abstract concepts. Morphological markers are deterministic and avoid hand-typed lists (suffix patterns only). Inverted-U reflects Wundt curve: optimal creativity at moderate concreteness, not maximal.",
    "code": "import re\nfrom wordfreq import word_frequency, top_n_list\nimport statistics\n\n_W = re.compile(r\"[a-zA-Z']+\")\nSTOP = set((\"a an the and or but if then else of to in on at by for with from as is are was were \"\n    \"be been being it its this that these those i you he she we they me him her us them my your \"\n    \"his their our not no so do does did have has had will would can could should may might must \"\n    \"about into over under out up down off than too very just also\").split())\n\ndef _toks(s):\n    return [t for t in _W.findall((s or \"\").lower())]\n\ndef _content(s):\n    return [t for t in _toks(s) if t not in STOP and len(t) > 1]\n\ndef morphological_concreteness_score(text, anchor):\n    if not text:\n        return 0.5\n    content_toks = _content(text)\n    if not content_toks:\n        return 0.5\n    abstract_suffixes = ('-ness', '-ity', '-ment', '-tion', '-ism', '-ship', '-age')\n    abstract_count = sum(1 for tok in content_toks if any(tok.endswith(s) and len(tok) > len(s) + 2 for s in abstract_suffixes))\n    abstract_ratio = abstract_count / len(content_toks)\n    optimal = 0.25\n    deviation = abs(abstract_ratio - optimal)\n    score = max(0.0, 1.0 - deviation)\n    return max(0.0, min(1.0, score))\n\nMETRICS = {\"morphological_concreteness_score\": morphological_concreteness_score}"
  },
  {
    "name": "ttr_novelty_conjunction",
    "causal_node": "lexical_diversity_ttr",
    "mechanism": "Type-Token Ratio gated by surprisal of unique vocabulary: TTR × (surprisal of unique word types). Lexical diversity alone shows weak/inverse correlation (causal graph); but diverse vocabulary using novel rare words signals elaboration. Conjunctive form: score = TTR * surprisal_signal. Citation: TTR caveats (Jarvis et al.); elaboration theory (Kintsch); novelty gates appropriateness (Rastelli et al. 2022).",
    "reasoning": "lexical_diversity_ttr is uncovered and noted with weak/inverse relationship to creativity. Measuring TTR alone would reinforce this inverse signal. Solution: gate TTR by surprisal (novelty) to reward strategic vocabulary expansion with novel rare words, not just padding with common synonyms. Conjunctive scorers survived evolutionary selection (per champion analysis). Deterministic: wordfreq surprisal calibrated at import via reference distribution.",
    "code": "import math\nimport re\nimport numpy as np\nfrom wordfreq import word_frequency, top_n_list\nimport statistics\n\n_W = re.compile(r\"[a-zA-Z']+\")\nSTOP = set((\"a an the and or but if then else of to in on at by for with from as is are was were \"\n    \"be been being it its this that these those i you he she we they me him her us them my your \"\n    \"his their our not no so do does did have has had will would can could should may might must \"\n    \"about into over under out up down off than too very just also\").split())\n\ndef _toks(s):\n    return [t for t in _W.findall((s or \"\").lower())]\n\ndef _content(s):\n    return [t for t in _toks(s) if t not in STOP and len(t) > 1]\n\ndef _word_surprisal(word):\n    p = word_frequency(word, \"en\")\n    if p > 0:\n        return -math.log2(p)\n    return 24.0\n\ndef _ref_surprisal_stats():\n    words = top_n_list(\"en\", 50000)\n    ps = np.array([word_frequency(w, \"en\") for w in words])\n    ps = ps[ps > 0]\n    ps = ps / ps.sum()\n    sur = -np.log2(ps)\n    mu = float((ps * sur).sum())\n    sd = float(math.sqrt((ps * (sur - mu) ** 2).sum()))\n    return mu, sd\n\n_SURP_MU, _SURP_SD = _ref_surprisal_stats()\n\ndef ttr_novelty_conjunction(text, anchor):\n    if not text or not anchor:\n        return 0.5\n    content_toks = _content(text)\n    if len(content_toks) < 2:\n        return 0.5\n    unique_tokens = len(set(content_toks))\n    ttr = unique_tokens / len(content_toks)\n    unique_surps = [_word_surprisal(w) for w in set(content_toks)]\n    mean_unique_surp = statistics.mean(unique_surps) if unique_surps else _SURP_MU\n    surp_signal = max(0.0, min(1.0, (mean_unique_surp - _SURP_MU) / (2.0 * _SURP_SD) + 0.5))\n    combined = ttr * surp_signal\n    return max(0.0, min(1.0, combined))\n\nMETRICS = {\"ttr_novelty_conjunction\": ttr_novelty_conjunction}"
  }
]
