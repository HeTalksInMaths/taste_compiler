import PanelShell from "./PanelShell";

/**
 * Placeholder panel for future agent evaluation metrics.
 */
export default function AgentBatteryPanel() {
  return (
    <PanelShell title="Agent Battery" hasData={false}>
      <p>Agent Battery: not run for this demo artifact</p>
    </PanelShell>
  );
}
